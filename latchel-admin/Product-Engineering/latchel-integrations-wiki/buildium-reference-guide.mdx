---
title: "Buildium Reference Guide"
groups: ["latchel"]
---

This document is the detailed source of truth for how the Latchel\<\>Buildium sync operates today.

**Technical Docs**: [Open API, powered by Buildium](https://www.buildium.com/open-api/)

<Warning>
  Customers must be on Buildium's **"Premium"** product to access the Open API feature that powers the Latchel\<\>Buildium integration.
</Warning>

## **Activating the integration**

## **Integration Requirements**

Customers must be on Buildium’s “Premium” product to access the Open API feature that powers the Latchel\<\> Buildium integration

[Pricing - Buildium](https://www.buildium.com/pricing/)

## **1 – Activate the API and obtain API Keys**

1. PM Should activate the API and obtain API keys as instructed in the Buildium Docs with the following permissions.<br />[Open API, powered by Buildium](https://developer.buildium.com/#section/Getting-Started)

<Frame>
  ![Buildium](/images/buildium.png)
</Frame>

2. In Latchel, navigate to the PM Company's Integrations tab, locate the Buildium card, and click "Activate." Enter the Client ID and Client Secret, then click Save.
   ![Image](/images/image-12.png "Image")

## **2 – Get Property & Resident Data from Buildium**

**NOTE**: This step is only necessary for existing customers. If you are working with a new customer with no data in Latchel yet, you can disregard steps 2 and 3.

This step is critical to avoid creating duplicate property and resident records in Latchel.

Request a CSV export of the “Current Tenants” report (Reports \> Current Tenants) from the Buildium Web App

<Frame>
  ![Image 20241209 192550](/images/image-20241209-192550.png)
</Frame>

### **3 – Submit a #task-share-jira-request**

In this step, the data support team will clean and prepare the customer’s data by assigning “ref\_ids” to each resident and property in Latchel. This will prevent the integration from accidentally creating those records again when we turn the integration on in the next stage.

1. Navigate to the #task-share-jira-request slack channel
2. Go to Workflows--\>Account Update Request
3. Fill out the required information
   1. Select “Data Migration” in the task type question
   2. Once the data support team completes the data work, activate the integration (see Step 1 above).

## **What happens when a customer first activates their integration?**

When an admin first activates the customer’s Buildium integration nothing happens immediately. The integration will be active and will be scheduled for hourly imports but currently does not have an “initial sync” like Propertyware and Rent Manager.

## **Buildium → Latchel**

## **Properties**

The following Property fields are brought over from Buildium to Latchel.

| **Buildium Field** | **Value/Interpretation in Latchel** |
| :-- | :-- |
| PropertyId | ref\_property\_id |
| UnitId | ref\_unit\_id |
| Address.Address1 | address1 |
| Address.Address2 | address2 |
| [Address.City](http://Address.City) | city |
| Address.State | state |
| Address.PostcalCode | zip |
| blank | criticalNote |
| property active status ingested for all child units | property\_is\_active |
| \[buildium\_unit\_id =\> UnitId, buildium\_property\_id =\> PropertyId | integration\_data |

### **Previously created Properties**

Subsequent synchronizations of the same property will impact the following fields:

| **Platform field** | **Field/Interpretation in Latchel** |
| :-- | :-- |
| (various data) | integration\_data |
| Address data | address\_label If any address data is different in the platform than in Latchel, we store the exact platform address in order to provide the “Source Address” feature for customers. [Link to product release.](https://latchel.slack.com/archives/C038Z6M0T6H/p1735848840482549 "https://latchel.slack.com/archives/C038Z6M0T6H/p1735848840482549") |
| [property.is](http://property.is)\_active | is\_active (only from active to inactive) **Note**: This means if a customer _activates_ an _inactive property_ in the platform, **this change will not propagate to Latchel.** |

**Note**: In Buildium, customers can mark a property as either active or inactive. That setting applies to all units in the property. So if a customer marks a property with multiple units as inactive in Buildium, all of the Latchel properties (units) will also be marked as inactive in Latchel.

#### **Automatic Property Reconciliation**

To ensure accurate customer billing Latchel runs on automated process each month to de-activate any active units in Latchel that no longer exist in Buildium. This process currently happens on the 27th at 3am PST.

<Frame>
  ![Yardi Reconcile Active Units Frame 5 (1) 20241212 211604](/images/Yardi-Reconcile-Active-Units---Frame-5-\(1\)-20241212-211604.jpg)
</Frame>

When a property is deactivated through this process. It will be logged to the property in Latchel

<Frame>
  ![Image 20231219 185054 (2)](/images/image-20231219-185054-\(2\).png)
</Frame>

### **Common areas**

Upon customer request, Latchel can create common area units for PM’s to manage building-level maintenance for jobs such as landscaping, garbage removal, elevator maintenance and more.

How it works:

1. A PM (or Latchel team member on behalf of a PM) submits a one-off request for common area maintenance on a specific building(s) via this Typeform: [https://latchel.typeform.com/to/P8jj3FBx](https://latchel.typeform.com/to/P8jj3FBx)
   1. Why one-off requests only? We only want to enable common areas on specific addresses for customers who are actually going to create building-level work orders due to billing implications (they will appear as active units in Latchel for billing purposes).
2. Latchel creates the common area unit via API.
   1. This results in a record with a building ID but no ref unit ID.

## **Residents**

The following resident fields are brought over from Buildium to Latchel. Currently we sync all eligible residents on every sync. This differs slightly from our other integrations where we only sync residents that were added or edited in the last 72 hours. An “eligible” resident is anyone living at the property who is not in the Buildium status `['Past']`. We sync all residents on the property so that any of them are able to create a work order in Latchel.

| **Buildium Field** | **Value/Interpretation in Latchel** |
| :-- | :-- |
| Id | ref\_user\_id |
| Firstname \+ Lastname | name |
| Email | email |
| Phone  -\> phone number prioritize: cell \> home \> work \> fax | Primary phone number\* |
| PropertyId | ref\_property\_id |
| UnitId | ref\_unit\_id |
| Move In Date | move\_in\_date |
| Move Out Date | move\_out\_date |

<Note>
  We need to set a primary phone number for the resident in Latchel. Our current logic states that when multiple numbers are provided, we will prioritize setting the primary phone number in the order listed in the table.
</Note>

### **Previously-created residents**

Once a resident is created we only change a few things on subsequent syncs

- Property/unit associations
- Move in / move out dates
  - When a resident is passed their move-out date, we’ll mark them inactive at the relevant property in Latchel

- Resident contact information - [#product-release](https://latchel.slack.com/archives/C038Z6M0T6H/p1787085428053079)
  - Name
  - Phone Numbers
  - Email Addresses
- Frequency: Hourly sync

## **Work orders**

By default, work orders are not imported from Buildium to Latchel. If CS or PS receive a specific customer request to activate work order imports, please submit a #feature-request and tag @Jordan Caley and @Ben Heller in the thread.

If a customer requests to activate work order imports, the Buildium integration syncs work orders that have been created in the last 72 hours.

| **Platform field** | **Field/Interpretation in Latchel** |
| :-- | :-- |
| Work order # | Not set\* \*See relevant Product Support note below |
| Id | ref\_job\_id |
| Task-\>Title | name |
| WorkDetails | Description |
| DueDate | scheduled\_start |
| Status\* | If the work order is vacant, set to “Scheduling with Vendor,” otherwise set  to “Diagnosing Issue” \*We don’t take the platform’s status into account when deciding which Latchel status to set when the WO is created. See rules above. |
| Priority | is\_urgent is true if Priority is “High” |
| EntryContact-\>Id | ref\_user\_id |

## **Latchel → Buildium**

## **Work orders**

Work orders are synced to Buildium a few seconds after they are created in Latchel.

| **Field in Latchel** | **Field/Interpretation in Platform** |
| :-- | :-- |
| order\_number | Not currently set\* \*See relevant Product Support note below |
| description | WorkDetails |
| enter\_permission | EntryAllowed |
| access\_instructions | EntryNotes |
| N/A - we generate a special value for this that doesn’t exist in Latchel\* | VendorId Latchel vendor (we create and add a “Latchel Vendor” for this purpose) |
| vendor\_description | VendorNotes |
| name | Task-\>Title |
| scheduled\_start | Task-\>DueDate |
| is\_emergency ? High, otherwise Normal | Task-\>Priority |
| state\_id - always set to the same value\* | Task-\>Status \> ‘InProgress’ \*Buildium API currently doesn't allow updating the work order so we are unable to update the state like we normally do on completion |
| ref\_property\_id | PropertyId |
| ref\_unit\_id | UnitId |
| N/A - this data doesn’t exist in Latchel\* | AssignedToUserId \*Always set to the primary user in the Buildium account |

#### **Work Order Update conditions**

A customer can specify if they want WO updates to happen less often than by default. This settings is located  on a PM company page → Integrations tab → Work Order Update conditions. This setting is only accessible by admin users.

<Frame>
  ![Screenshot 2026 07 01 At 4 06 54 P M](/images/Screenshot-2026-07-01-at-4.06.54-p.m.png)
</Frame>

## **Files**

We don’t sync files to Buildium yet

## **Product Support FAQs**

1. **Can you sync the Latchel work order number to match Buildium’s?**

We would love to support this feature!  Unfortunately the Buildium API doesn’t currently allow us to read the Buildium work order number. You can help us convince Buildium to enable this feature by sending this note to your Buildium account representative or submitting a support request:

> “Please allow us to retrieve task and work order numbers from the API. We need this data to be able to visually reference work orders between systems”

 

2. **Can you automatically update the work order/task status in Buildium when the work order is complete in Latchel?**

We would love to support this feature!  Unfortunately the Buildium API doesn’t currently allow us to update the status of a task or work order. You can help us convince Buildium to enable this feature by sending this note to your Buildium account representative or submitting a support request:

> “Please allow us to update the task and work order status via API. We need this functionality to be able to keep work orders updated in Buildium.”

3. **We only want work orders to sync from Buildium to Latchel, not the other direction. How do we do it?**

In Buildium, go to Settings \> Developer Tools and locate the API key created for Latchel and click the three dots to edit.

For the “Maintenance” permission, leave “VIEW” checked but uncheck “EDIT” just like the screenshot below. Thats it! Now work orders will only sync from Buildium to Latchel.

<Frame>
  ![Image 20250604 192427](/images/image-20250604-192427.png)
</Frame>